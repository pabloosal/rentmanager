<UserControl x:Class="RentManager.Views.ContratosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#2D6CDF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2D6CDF"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Gestión de Contratos"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#E2E2E2"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12"
                Margin="0,0,0,12">

            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <TextBox x:Name="txtBuscar"
                             Width="260"
                             Height="32"
                             Margin="0,0,10,0"
                             VerticalContentAlignment="Center"
                             TextChanged="TxtBuscar_TextChanged"/>

                    <Button Content="Buscar"
                            Style="{StaticResource ActionButtonStyle}"
                            Click="BtnBuscar_Click"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" Cursor="">                  
                    <Button Content="Añadir"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="BtnAñadir_Click"/>

                    <Button Content="Editar"
                            Style="{StaticResource ActionButtonStyle}"
                            Click="BtnEditar_Click"/>

                    <Button Content="Eliminar"
                            Style="{StaticResource ActionButtonStyle}"
                            Click="BtnEliminar_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#E2E2E2"
                BorderThickness="1"
                CornerRadius="8"
                Padding="8">

            <DataGrid x:Name="dgContratos"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      GridLinesVisibility="None"
                      RowHeaderWidth="0"
                      CanUserAddRows="False">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Vivienda" Binding="{Binding ViviendaDireccion}" Width="2*"/>
                    <DataGridTextColumn Header="Inquilino" Binding="{Binding InquilinoNombreCompleto}" Width="2*"/>
                    <DataGridTextColumn Header="Inicio" Binding="{Binding FechaInicio}" Width="*"/>
                    <DataGridTextColumn Header="Fin" Binding="{Binding FechaFin}" Width="*"/>
                    <DataGridTextColumn Header="Renta" Binding="{Binding RentaMensual}" Width="*"/>
                    <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
